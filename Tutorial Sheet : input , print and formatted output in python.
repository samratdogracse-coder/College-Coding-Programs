LEVEL 1 - VERY BASIC: USING print()

#1. Write a program to display the message : Hello,Python!

print('Hello,Python!')


#2. Write a program to print your name,roll number,course,semester and collegename on five seperate lines.

name = 'samrat'
roll_number = 66
course = 'btech-cse'
semester = 'Ist'
college_name = 'MIET'
print(f'Name : {name}')
print(f'Roll number : {roll_number}')
print(f'Course : {course}')
print(f'Semester : {semester}')
print(f'College Name : {college_name}')


#3. Write a program to display the following three lines using threee print() statements: Welcome to Python/semester1/cse department

print('Welcome to Python')
print('Semester 1')
print('CSE Department')


#4. Store your name in a variable and print the value of the variable.

name = 'samrat dogra'
print(f'My Name is : {name}')


#5. Create three variables a = 10 , b = 20 and c = 30 . Display all three values using a single print() statement.

a = 10
b = 20
c = 30
print( a , b , c )


#6. Create variables subject = 'python' and semester = 1 . Print both values with suitable labels using print()

subject = 'Python'
semester = 1
print(f'Subject = {subject}')
print(f'Semester = {semester}')


LEVEL 2 - BASIC INPUT : USING input()

#7. Take the students name as input and display : Welcome,<name>

student_name = input('Enter the student name : ')
print(f'Welcome, <{student_name}>!')


#8. Take name , roll number and section as input and display all details clearly.

name = input('Enter the name : ')
roll_number = int(input('Enter roll number : '))
section = input('Enter the section : ')
print(f'Name : {name}')
print(f'Roll number : {roll_number}')
print(f'Section : {section}')


#9. Take two integer values from the user and display their sum.

num1 = int(input('Enter the first number : '))
num2 = int(input('Enter the second number : '))
sum = num1 + num2
print(sum)


#10. Take length and breadth of a rectangle as input and display their sum.

length = int(input('Enter the length : '))
breadth = int(input('Enter the breadth : '))
area = length * breadth
print(f'Area : {area}')


#11. Take a floating - point number as input and display the number back to the user with suitable message.

bill_amount = float(input('Enter the bill : '))
print(f'Total bill = {bill_smount}')


#12. Take item name , price and quantity as input . Calculate and dispklay the total amount (price * quantity).

item_name = input('Enter the item name : ')
price = int(input('Enter the price : '))
quantity = int(input('Enter the quantity : '))
total_amount = price * quantity
print(total_amount)


LEVEL 3 - f-STRINGS


#13. Take name and age as input and display: My name is <name> and i am <age> years old. Use an f-string.

name = input('Enter the name : ')
age = int(input('Enter the age : '))
print(f'My name is <{name}> and I am <{age}> years old.')


#14. Take two numbers as input and display the complete addittion statement in the form : 12+8=20. Use an f-string.

num1 = int(input('Enter the first number : '))
num2 = int(input('Enter the second number : '))
sum = num1 + num2
print(f'{num1}+{num2}={sum}')


#15. Take two numbers a and b as input . Display their sum , difference , product and quotient using seperate f-string statements.

a = int(input('Enter the first number : '))
b = int(input('Enter the second number : '))
sum = a+b
difference = a-b
product = a*b
quotient = a/b
print(f'Sum : {sum}')
print(f'Difference : {difference}')
print(f'Product : {product}')
print(f'Quotient : {quotient}')


#16. Take length and breadth as input, calculate the area of rectangle , and display : area of rectangle = <area> . Use an f-string

length = int(input('Enter the length : '))
breadth = int(input('Enter the breadth : '))
area = length*breadth
print(f'Area pf Rectangle = {area}')


#17. Take a students name and marks in three subjects. Calculate the total and average , and display the students name , total and average using f-strings.

student_name = input('Enter the student name : ') 
maths_marks = int(input('Enter the maths marks : '))
english_marks = int(input('Enter the english marks : '))
design_marks = int(input('Enter the design marks : '))
total = maths_marks + english_marks + design_marks 
average = total / 3
print(f'Student Name : {student_name}')
print(f'Total : {total}')
print(f'Average : {average}')


#18. Take item name , unit price and quantity as input . Display a small bill showing item , price , quantity and total using f-strings.

item_name = input('Enter the item name : ')
price = int(input('Enter the price : '))
quantity = int(input('Enter the quantity : '))

print(f'Item name : {item_name}')
print(f'Price : {price}')
print(f'Quantity : {quantity}')


#19. Take a current age of a student as input and display the age next year directly as an expression inside an f-string.

age = int(input('Enter the current age : '))
age1 = age + 1
print(f'Age Next year : {age1}')


#20. Take a radius as input , calculate the area of a circle using 3.14159 * radius * radius , and display the result using an f-string.

radius = int(input('Enter the radius of the circle : '))
area_of_circle = 3.14159 * radius * radius 
print(f'Area of circle : {area_of_circle}')


LEVEL 4 - .format() and % FORMATTING

#21. Take a name as input and print hello <name> using the format() method .

name = input('Enter the name : ')
print('Hello {}'.format(name))


#22. Take name and age as input and print them in one sentence using two {} placeholders with .format().

name = input('Enter the name : ')
age = int(input('Enter the age : '))
print('Name = {} , Age = {}'.format(name,age))


#23. Take name and age as input. Use postional placeholders so that the age is displayed before name

name = input('Enter the name : ')
age = int(input('Enter the age : '))
print('Age = {},Name = {}' .format(age,name))








